# Birlik Digital Bank

Цифровая банковская платформа для Казахстана, СНГ и Азии с поддержкой трансграничных переводов без SWIFT.

## Особенности

- Трансграничные переводы без SWIFT
- Мультивалютная поддержка (Фиат, CBDC, Крипто)
- Мгновенное зачисление (3-5 секунд)
- Низкие комиссии (0.1-0.3%)
- Верификация через блокчейн
- NFT-паспорта для цифровой идентификации
- Децентрализованное управление (DAO)
- Интеграция с платформой Birlik Live

## Структура проекта

```
birlik-digital-bank/
├── backend/              # FastAPI бэкенд
│   ├── app/              # Основной код приложения
│   │   ├── core/         # Конфигурация и утилиты
│   │   ├── modules/      # Модули приложения
│   │   │   ├── auth/     # Аутентификация и авторизация
│   │   │   ├── dfmi/     # Цифровые деньги и платежи
│   │   │   ├── identity/ # Цифровая идентификация
│   │   ├── services/     # Сервисы приложения
│   │   ├── integrations/ # Интеграции с внешними сервисами
│   │   │   ├── birlik_live/ # Интеграция с Birlik Live
│   ├── .env              # Переменные окружения
│   ├── pyproject.toml    # Зависимости и конфигурация Poetry
│
├── frontend/             # React фронтенд
│   ├── src/              # Исходный код
│   │   ├── components/   # React компоненты
│   │   ├── contexts/     # React контексты
│   │   ├── pages/        # Страницы приложения
│   │   ├── services/     # Сервисы для работы с API
│   │   │   ├── integrations/ # Интеграции с внешними сервисами
│   ├── .env              # Переменные окружения
│   ├── package.json      # Зависимости и скрипты
│
├── documentation/        # Документация проекта
```

## Требования

### Бэкенд
- Python 3.10+
- Poetry
- SQLite (для разработки)
- PostgreSQL (для продакшена)

### Фронтенд
- Node.js 18+
- npm или yarn

## Установка и запуск

### Бэкенд

1. Перейдите в директорию бэкенда:
   ```bash
   cd backend
   ```

2. Установите зависимости с помощью Poetry:
   ```bash
   poetry install
   ```

3. Создайте файл .env на основе примера:
   ```bash
   cp .env.example .env
   ```

4. Запустите сервер разработки:
   ```bash
   poetry run uvicorn app.main:app --reload
   ```

5. Бэкенд будет доступен по адресу: http://localhost:8000

### Фронтенд

1. Перейдите в директорию фронтенда:
   ```bash
   cd frontend
   ```

2. Установите зависимости:
   ```bash
   npm install
   ```

3. Создайте файл .env на основе примера:
   ```bash
   cp .env.example .env
   ```

4. Запустите сервер разработки:
   ```bash
   npm run dev
   ```

5. Фронтенд будет доступен по адресу: http://localhost:5173

## API документация

После запуска бэкенда, документация API будет доступна по адресу:
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

## Интеграция с Birlik Live

Платформа интегрируется с Birlik Live для предоставления дополнительных сервисов:
- Карты и геолокация
- Маркетплейс
- Социальная сеть

Для настройки интеграции необходимо указать API ключи в файле .env бэкенда:
```
BIRLIK_LIVE_API_KEY=your-birlik-live-api-key
BIRLIK_LIVE_API_SECRET=your-birlik-live-api-secret
```

## Лицензия

Copyright © 2025 Birlik Digital Bank
